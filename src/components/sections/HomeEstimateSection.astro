---
import EstimateForm from '../forms/EstimateForm.astro';
import { site } from '../../data/site';
---

<section class="py-16 md:py-20 bg-white">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-10 lg:gap-16 items-start">

      <!-- LEFT COLUMN: Context + Trust -->
      <div class="pt-2">
        <p class="text-xs font-semibold uppercase tracking-wider text-brand-teal mb-3">Free Estimate</p>
        <h2 class="text-3xl md:text-4xl font-bold text-brand-slate mb-4 leading-tight">
          Get a Free Estimate From Wheyland Electric
        </h2>
        <p class="text-base text-text-muted leading-relaxed mb-8">
          Tell us about your project and we'll follow up with next steps, scheduling, and a detailed estimate — no obligation, no pressure.
        </p>

        <ul class="space-y-4 mb-8">
          <li class="flex items-center gap-3">
            <div class="w-8 h-8 rounded-full bg-surface-DEFAULT flex items-center justify-center flex-shrink-0" aria-hidden="true">
              <svg class="w-4 h-4 text-brand-slate" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"/>
              </svg>
            </div>
            <span class="text-sm font-medium text-text">Licensed, bonded &amp; insured — {site.license}</span>
          </li>
          <li class="flex items-center gap-3">
            <div class="w-8 h-8 rounded-full bg-surface-DEFAULT flex items-center justify-center flex-shrink-0" aria-hidden="true">
              <svg class="w-4 h-4 text-brand-slate" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
              </svg>
            </div>
            <span class="text-sm font-medium text-text">1-year labor warranty on all work</span>
          </li>
          <li class="flex items-center gap-3">
            <div class="w-8 h-8 rounded-full bg-surface-DEFAULT flex items-center justify-center flex-shrink-0" aria-hidden="true">
              <svg class="w-4 h-4 text-brand-slate" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
              </svg>
            </div>
            <span class="text-sm font-medium text-text">Response within one business day</span>
          </li>
        </ul>

        <div class="flex items-center gap-2 text-sm text-text-muted">
          <span>Prefer to call?</span>
          <a
            href={`tel:${site.nap.phoneTel}`}
            class="font-bold text-brand-slate hover:text-brand-teal transition-colors text-base"
          >
            {site.nap.phone}
          </a>
        </div>
      </div>

      <!-- RIGHT COLUMN: EstimateForm rendered directly — section provides the layout -->
      <div class="bg-white rounded-2xl border border-surface-subtle shadow-sm p-6 md:p-8">
        <header class="mb-5 space-y-1">
          <h3 class="text-xl font-heading font-bold text-brand-slate">Request a Free Estimate</h3>
          <p class="text-sm text-text-muted">We’ll respond within one business day.</p>
        </header>
        <EstimateForm />
      </div>

    </div>
  </div>
</section>
